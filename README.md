# HOMELAB

## Prerequisites

1. Git

## Usage & installation

1. install `git` then clone the repo to $HOME dir
2. run the installation script

```sh
cd $HOME/homelab
chmod +x ./install
./install
```
